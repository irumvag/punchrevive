# PunchRevive Environment Variables
# Copy this file to .env.local for local development
# Add these variables to Vercel project settings for production deployment

# Required: Supabase Database Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional: AI API Keys (for advanced features)
# If not provided, the app will work in limited mode with local processing
OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Optional: Vercel KV (Redis) for Shareable Links
# Leave empty if you don't need shareable links feature
KV_REST_API_URL=https://your-kv-store-url
KV_REST_API_TOKEN=your-kv-token
KV_REST_API_READ_ONLY_TOKEN=your-kv-read-only-token

# Optional: Public App URL (for shareable links)
# Set this to your production domain in Vercel
NEXT_PUBLIC_APP_URL=http://localhost:3000
